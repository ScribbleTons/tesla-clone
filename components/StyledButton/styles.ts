import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 6
    },
    button: {
        borderRadius: 50,
        padding: 10,
        textAlign: "center",
        fontSize: 16,
        textTransform: "uppercase",
    },
    primary: {
        backgroundColor: "#5c5b62",
        color: "#fff",
    },
    default: {
        backgroundColor: "#fff",
        color: "#5c5b62",
    },
})

export default styles